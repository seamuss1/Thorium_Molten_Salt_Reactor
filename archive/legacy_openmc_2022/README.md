# Legacy OpenMC Archive (2022)

This folder is the project museum: it preserves the original thesis-era scripts, XML exports, plots, and statepoint files that were previously stored at the repository root.

The active workflow now lives under `src/thorium_reactor`, `configs/cases`, and `results`. Nothing in this archive is used by the new CLI-driven pipeline.
