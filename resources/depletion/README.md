# Depletion Chains

`tiny_thorium_chain.yaml` is a deliberately small native depletion-chain fixture
for local tests, examples, and dry-run bundles. It is not a production burnup
library. Realistic chains should be imported from OpenMC depletion XML when the
source data and validation basis are available.
